<div {{ attributes.defaults(stimulus_controller('modal-add-people')) }}>
    <div class="modal-body">
        <div class="mb-3">
            <label class="form-label" for="selectPeople">Emails</label>
            <select class="form-control" id="selectPeople"></select>
        </div>
    </div>
    <div class="modal-footer">
        <button class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        <button class="btn btn-primary" id="buttonAddPeople">Add</button>
    </div>
</div>