{% extends 'base.html.twig' %}

{% block title %}{{ project.name }} Board{% endblock %}

{% block body %}
    <div class="container mt-3">
        {% include 'breadcrumb.html.twig' with { breadcrumbs: ['Projets', project.name, 'Tableau de bord'] } %}


        {{ component('ProjectBoard') }}
    </div>
{% endblock %}